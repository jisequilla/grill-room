# Intent: Export by lifetime + ADR drafts

Split Grill Room's export by lifetime, and export ADR-worthy decisions as draft ADRs.

Today export-session writes one bundle into one project export folder: spec.md, decisions.md, intent.md (durable: they explain the system after it is built) next to issues/, HANDOFF.md, briefs/ and the manifest (working files that go stale once the tickets merge). The owner decided the export should not default to .scratch/, which is for impermanent work and is often gitignored, hiding the bundle from worktrees.

Part 1 (gr-0hy): durable files go to the repo's docs folder (e.g. docs/specs/<slug>/), working files go to a tracked .grill-room/<slug>/ with the manifest, so .grill-room/<slug>/ can be deleted after the build without losing anything anyone reads. Touches project settings (two folders, or one folder plus a docs folder), the export plan and manifest (files across two roots), the edited-file guard, visibility checks, HANDOFF paths and briefs.

Part 2 (gr-2f9): on ngine-monitor the architecturally significant decisions (read model, liveness, labels/roles, benchmark semantics) had to be written into an ADR (NMON-019) by a ticket, from scratch. Idea: Grill Room marks each settled decision ADR-worthy or not (significant and costly to reverse vs product/delivery detail), and the export writes draft ADRs in the repo's own convention, detected by the scout (decisions folder, numbering like NMON-###, template), with Status Proposed, Context from the question and evidence, Decision from the answer, Alternatives from the unchosen options and superseded answers, Consequences, and links to the tickets that build it. The building session accepts the drafts when tickets land. Open: one decision per ADR vs bundled (a single NMON-019 bundling read model, liveness and labels makes partial supersession awkward; liveness also amends NMON-012).

## Readiness

Not judged for this version of the idea.

## Project state

- **Built:** A project has exactly one `exportFolder` (schema field), so today's export writes durable files (spec.md, decisions.md, intent.md) and working files (issues/, HANDOFF.md, briefs/, manifest) into the same bundle directory under one root — there is no second, docs-style destination. (grill-room/server/db/schema.ts:1, grill-room/server/projects.ts:75-98)
- **Built:** `planExportBundle`/`writeExportBundle` is the single source of truth for the bundle layout, listing exactly the files the idea calls working files (spec.md, intent.md, decisions.md, issues/NN-slug.md, HANDOFF.md, briefs/NN-slug.md, .grill-room-export.json) as one flat set under `<root>/<exportFolder>/<folderName>/`. (grill-room/server/export-bundle.ts:1-18)
- **Built:** The export gate, edited-file guard, manifest and containment checks are already keyed to a single bundle directory and a single manifest file, so splitting output across two roots would touch all of this machinery. (grill-room/server/export-bundle.ts:119-151)
- **Built:** Project registration already seeds and stores a `visibility` flag (`tracked`/`ignored`) from `git check-ignore` on the export folder, and the project owner's decision that export must not silently land somewhere gitignored is reflected in this flag plus the visibility-mismatch warnings, though it applies to one folder, not two. (grill-room/server/projects.ts:195-207, grill-room/server/visibility.ts:193-206)
- **Gap:** There is no second project setting for a docs destination that export writes into (e.g. `docs/specs/<slug>/`) separate from the working `exportFolder`; `ProjectInput`/`validate()` only knows `exportFolder`. (grill-room/server/projects.ts:75-98)
- **Built:** Sessions already carry a separate, unrelated `docsFolder` (a read-only folder the interviewer may read while grilling) with its own validation, refusal codes and UI chip — a different concept from an export destination, but the closest existing 'docs folder' naming in the codebase that a Part 1 implementation would need to avoid colliding with. (grill-room/actions/set-docs-folder.ts:12-18)
- **Built:** The scout already detects a project's decisions/ADR folder by checking a fixed candidate list (`docs/decisions`, `docs/adr`, `adr`) and records it as `decisionsFolder` in the stored server facts, plus whether an agent-rules folder exists. (grill-room/server/project-facts.ts:61-84)
- **Partial:** Detection only checks for the folder's existence at three fixed candidate paths; it does not detect an ADR numbering convention (e.g. `NMON-###`) or an existing ADR template the way Part 2's scout would need to. (grill-room/server/project-facts.ts:61-84)
- **Built:** The scout already proposes 'repo decisions' with exactly the shape Part 2 needs for an ADR's provenance: a key, title, statement, `recorded`/`inferred` source, a `path:line` citation, and a one-line reason, checked against the project's real files before acceptance. (grill-room/server/scout-report.ts:301-315)
- **Gap:** Nothing in the scout, the design tree, or the decision schema marks a settled decision as ADR-worthy or not (significant/costly-to-reverse vs. a product or delivery detail); no such field exists on decisions or scout proposals. (grill-room/server/tree.ts:364-393)
- **Partial:** `decisions.md` rendering already assembles most of an ADR's body from a settled decision — its question/title as context, its accepted answer or own answer as the decision, the interviewer's rationale, its dependencies, and (for a repo-origin decision) the source citation and the exact statement it supersedes — but as one Markdown file for the whole session, not one draft file per decision in Status/Context/Decision/Alternatives/Consequences form. (grill-room/server/export.ts:523-568)
- **Gap:** There is no 'Consequences' concept anywhere in the decision model or renderer, and no link from a decision to the tickets that build it (tickets reference a spec/ticket number, not a decision). (grill-room/server/export.ts:27-33)
- **Built:** A decision's unchosen options are already modelled and available: `choices` on a `DecisionView` holds every offered choice with its label and rationale, distinct from the one the answer picked, which is exactly the 'unchosen options' source Part 2's Alternatives section would draw from. (grill-room/server/tree.ts:364-369)
- **Built:** Grill Room's own `decisions.md` format already models one decision superseding another (a `Supersedes:` line naming the citation it overrides) and a parser (`supersededEntries`) that reads that back out of a tracked file — a working precedent for the 'amends' problem Part 2's open question raises (a liveness ADR amending NMON-012), though it operates on Grill Room's own file, not a target repo's ADR convention. (grill-room/server/export.ts:562-565)
- **Built:** A decision's DB row already stores a `supersededById` link to another decision, so one decision superseding another is a first-class relationship in the design tree, not just a rendering convention. (grill-room/server/tree.ts:459-462)
- **Gap:** No code detects or renders an ADR in a target repo's own numbering/template convention (e.g. `NMON-###`); the only ADR-shaped rendering that exists (`renderDecisionsFile`) is Grill Room's fixed internal format, not one the scout infers per-project. (grill-room/server/export.ts:586-632)
- **Built:** Post-export visibility classification and its warnings are already generic over an arbitrary set of absolute paths under the project root, so extending them to a second root (a docs folder) would reuse this function rather than needing a new one. (grill-room/server/visibility.ts:224-239)
- **Built:** The edited-file guard already treats 'stored hash from the previous manifest' as the sole basis for what counts as hand-edited and therefore kept rather than overwritten — the same guard Part 1 would need to apply independently across two roots and Part 2 would need for 'accept the drafts when tickets land' (an accepted ADR draft becoming a file the export must no longer own/overwrite). (grill-room/server/export.ts:116-122)

- **Commit read:** 3003fa3499c557ee7a3a06d03becc01912ed3f55

The project has changed since this report was read.
