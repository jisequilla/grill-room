# Delegation Log

One row per delegated task. Bead close comments hold the detail; this table is the summary.

| Task | Model | First attempt passed | Escalated | What the prompt was missing |
|------|-------|----------------------|-----------|-----------------------------|
| Spike: Claude Code harness (`docs/spikes/claude-code-harness.md`) | opus | Yes: all six questions answered with evidence; the fallback claim reproduced independently by the main session | No | No limit on the permissions of the nested agent under test, which ran `kill` and `rm` on the host. No instruction to keep provisioned tokens out of command output; one was exposed and rotated. No instruction to remove throwaway files, so the main session cleaned up. |
| gr-01 App shell, chat removal, test harness | opus | Yes: tests 4/4, typecheck 0, live route and action checks by the main session | No | No leads on how the framework resolves its database handle. The agent asked the spike agent, which supplied unexecuted source-reading that turned out right. Peer help, not the prompt, closed the gap, so this pass overstates the prompt. |
| gr-02 and gr-03, first launch | sonnet, opus | No: both killed by a session restart before any commit | No | Nothing: an infrastructure failure, not a model or spec failure. Branches were empty, beads held the claims, relaunch was clean. Prompts now say to commit early and often. |
| gr-02 Schema and session actions | sonnet | Yes: tests 22/22, typecheck 0, scope clean, verified in its worktree | No | Did not say the test harness runs each migration entry as one prepared statement, so multi-statement entries break every test. The agent found and worked around it; filed as gr-16. |
| gr-01, unrequested follow-up | opus | n/a | n/a | The ticket 01 prompt did not say "when you have reported, stop". After its work was merged the agent found a real harness bug, switched the main checkout to a new branch and committed a fix there. The fix was good (kept, with its regression test), but the main session's next merge silently landed on that branch instead of main and the bead was closed with a false "merged to main". Caught on the next worktree listing, main fast-forwarded after re-verifying 26/26. Prompts now forbid work after the report and any git command outside the agent's worktree; the main session now checks the current branch before every merge. |
| gr-03 Interviewer port | opus | Yes: 38 new tests, typecheck 0, skill copy byte-identical, no test spawns, and one real turn run by the main session returned a valid 12-decision round | No | The verified CLI flags I supplied put the prompt in argv; a prompt starting with the skill's `---` frontmatter is parsed as an option. The agent hit it on a manual run and fixed it. My latency figure (seconds) came from a trivial probe; a real round took 68s. Spec corrected. |
| gr-11 Session list UI | sonnet | Yes for its scope: 64/64, typecheck 0, HTTP flow re-checked. Rendering NOT verified: the Chrome extension was unavailable (gr-18) | No | Nothing blocking. It could not import enum constants into client code and schema.ts was out of bounds, so it duplicated them (gr-19). |
| gr-02, post-merge defect | sonnet | Revised to NO: tests passed but the app returns 500 on any fresh real database | n/a | Neither the spec nor the prompt said the framework owns tables in the same database; its auth layer has a `sessions` table, so ours was silently never created. The harness builds only app tables, so tests could not see it, and the main session did not boot the app on a fresh database before closing the bead. Found by the ticket 11 agent (which misdiagnosed it as a migration race) and root-caused by the main session. Fix: prefix all app tables `gr_` (gr-17, given to the ticket 04 agent, which owns the schema files). Verification now includes booting the real app on a fresh database after every merge that touches schema. |
| gr-04 Tree engine | opus | Yes: 103/103 in the worktree and on merged main, typecheck 0, scope clean, every specified rejection case has a test | No | Three conflicts inside my own prompt: "leave the database untouched on rejection" against "record turn status first" and "store the conversation id"; and "test/ is off-limits" against "pnpm test exits 0" once test files outnumbered CPU cores (gr-20). A scope addition sent by message mid-task (the gr-17 rename) was only read after the agent had sent its report, by which time the main session had merged its branch and removed its worktree; the agent correctly refused to work anywhere else and handed over its findings. A message to a running agent is not a delegation; scope changes need their own ticket. |
| gr-17, gr-20, gr-16, gr-19 Infrastructure fixes (one ticket) | opus | Yes: 127/127, typecheck 0, scope exact; real boot on main with an empty database showed 0 failed migrations; two real interviewer rounds then ran through the live app | No | Said 17 test files where there were 14. Steered the harness fix toward a cheaper reset when the measured cause was PGlite boot contention (11s) not rebuild cost (40ms); the agent measured first and fixed the real cause. Framed the regression test as either/or; the agent found only one framework migration list is public and did both. Did not warn that the framework's migration runner calls process.exit on failure unless wrapped. |
| Core loop defect found by the main session's live check | n/a | n/a | n/a | Tickets 03 and 04 each passed their own tests, but my two prompts left a hole between them: a decision added as blocked can never be asked once it unblocks (key re-proposal is rejected, the port cannot ask an existing decision, the app only opens rounds from new ask:true decisions). The frontier could never empty. Invisible to unit tests because each suite encodes its own half of the contract; found only by running two real rounds through the live app. Filed as gr-21. |
| gr-21 Orphaned frontier decisions | sonnet | Yes: 132/132, typecheck 0, scope exact, and live proof on the real session that exposed the bug (round 3 asked both orphans first, nothing left outside the round) | No | None reported. The tightest prompt so far (exact files, exact behaviours, exact test cases) on the cheapest model gave the cleanest result. |
| gr-06 Reopening and stale review | opus | Yes: 146/146 in the worktree and on merged main, typecheck 0, the one-call seam in submit-round held exactly. Live check deferred until gr-05 merges | No | My prompt was wrong on a mechanism: it assumed stale decisions exist after a round is settled, but submit-round clears reopenedAt on every card it settles, erasing the staleness. My own seam rule forbade the agent from fixing that line, so it restores the mark from decision history and named the cleanup (gr-22). No column existed for a review verdict's reason and schema changes were forbidden, so it lives in an unread field (gr-22). The seam bought a clean parallel merge at the price of duplicated turn bookkeeping. |
| gr-05 Steering moves and user-added decisions | sonnet | Yes for its own scope: 157/157 and typecheck 0 alone; on main after integration 178/178, plus a live round with the real model exercising push back, unknown, defer and a user-added decision | No | None reported. Two things surfaced only at integration: its new states broke ticket 06's duplicated snapshot builder (typecheck), and it stored a push back's interviewer explanation in the history row's answer field, overwriting the user's own reason (fixed in gr-22). No dedicated test for the restructure response. |
| gr-22 Integration of 05 and 06, and cleanup | opus | Yes: 178/178 and typecheck 0 on the branch and on main; live reopen and stale review with the real model (2 dependents stale, review turn then proposal turn, both re-asked with coherent reasons) | No | None reported. This ticket is the bill for running 05 and 06 in parallel on shared files: it removed the duplicated turn bookkeeping I ordered, found four behavioural differences between the copies, removed a workaround that would have produced false staleness, and wrote the interaction tests neither ticket could write alone. |
| gr-07 Done gate, dispositions, confirmation | sonnet | Yes: 202/202 and typecheck 0 on the branch and on main; live with the real model the interviewer validly proposed done after four more rounds, confirmation was refused with the loose end listed, and succeeded after a disposition | No | One of my two done-gate conditions was redundant (implemented anyway, flagged). I scoped "mark the spec out of date" to reopen-decision only, so add-decision on a confirmed session leaves the spec current (gr-23). |
| gr-12 Workspace UI | opus | Yes: scope exact, 185/185, typecheck 0; browser check by the main session on the real database and real CLI (thinking panel driven by stored status, six real cards arriving with no reload, drafts surviving reload, tree, history, detail panel; zero app console errors) | No | I scoped the done, loose-ends and confirm UI out because ticket 07 was in flight, and no other ticket covers it (gr-24): a hole in my ticket breakdown. Giving the agent a headless browser and the fake interviewer paid off: it found and fixed five visual defects itself before reporting. Process fault of mine: I merged before running the combined suite, and the first run showed a failure that turned out to be a flaky ticket 02 test (gr-25). |
| gr-08 Spec synthesis and tickets (with gr-23) | sonnet | Yes: combined suite 242/242 and typecheck 0 before committing the merge; live with the real model the app turned the confirmed session into a seven-section spec with 35 user stories (105s) and 18 tickets with a valid dependency graph (75s) | No | Did not specify break-into-tickets' return shape or the separator used when formatting dispositions; the agent chose sensibly and flagged both. |
| gr-09 Export | sonnet | Yes: combined suite 263/263 and typecheck 0 before committing; live export of the real session wrote spec.md and 18 issue files in the tracker layout, refused a relative path, and the overwrite guard wrote nothing while listing all 19 existing files | No | None reported. The format of ticketsSkippedReason was unspecified. |
| gr-10 Build records | sonnet | Yes: combined suite 284/284 and typecheck 0 before committing; live on the real session both the HTTP and command-line forms logged records, the summary was right, and ticket regeneration was then refused because it would delete build records | No | None reported. It established that the action command line forwards to the running dev server, since the embedded database refuses a second opener. |
| gr-24 Done, loose ends, confirm UI (with gr-26 polish) | opus | Yes: combined suite 302/302 and typecheck 0 before committing; screenshots reviewed by the main session; safe markdown renderer with an injection test | No | The fake interviewer cannot produce the states the UI had to show, so the agent seeded database rows, which only works with the dev server stopped (embedded database lock). Playwright must be required by absolute path. Both now go into UI prompts. |
| gr-13 Output UI | sonnet | Yes: combined suite 302/302 and typecheck 0 before committing; screenshots reviewed | No | The fake interviewer's canned queue is one per server process, so a second session on the same dev server desyncs it; now in the smoke-test prompt. |
| gr-14 Playwright smoke test | sonnet | Yes: unit 302/302, typecheck 0, and the e2e run by the main session passed in 12.6s; three cold runs by the agent at 12.4-12.8s | No | The agent withheld its commit because the Beads block in CLAUDE.md says conservative git policy; my prompt should state that it is the orchestrator instruction that block defers to. My boilerplate told it to wait for a loading splash that this framework version does not show. |
| gr-27 Workspace UX fixes (ten items from the design review) | opus | Yes: 315/315, typecheck 0, e2e passing, screenshots reviewed; D3S1GN3R verified 7 pass, 2 pass with note, 2 code-only | No | The recommendation-to-chip matcher I specified is a prefix match over prose the interviewer paraphrases; it fired on 1 of 5 live rounds. The right fix is at the port (gr-28), which was out of the ticket's bounds. Toolkit's outline variant has no border; one criterion was unachievable with the sidebar open. |
| gr-25 Ordering tie-breaks | sonnet | Yes: combined suite 316/316 and typecheck 0 before committing; the flaky test went from 7/40 to 0/40 failures, confirmed by the main session's own 20-run loop | No | None reported. The agent audited every ordered query and left the provably unique ones alone. It sat on finished work for a long time before committing. |
| gr-28 Choices with rationale, recommendation marked at the port | opus | Yes: combined 329/329, typecheck 0, e2e passing before committing; merged cleanly over ticket 25's edits to the same two files; real-model proof returned a rationale per choice and correct indexes; legacy rows verified on the user's live database | No | None reported. The one change that ran from port to schema to card, fully specified, landed clean. |
| gr-29 Superseded loose ends | opus | Yes: 353/353, typecheck 0, e2e passing before committing; real-model proof superseded the right loose end with a precise answer and left the deferred one alone | No | My prompt added a port method while putting the adapter off-limits; the agent crossed the line by three lines and said so. Ticket 07's panel ordering (failed before done-proposed) made 'record the turn as failed' hide the done panel; the agent chose idle-plus-error and tested it. A settled-includes-dispositioned edge case filed as gr-34. |
| gr-30 What-changed digest | sonnet | Yes: 367/367, typecheck 0, e2e passing on the merged tree; screenshot reviewed | No | None reported. One seam cost at the merge: gr-29 added a required field to DecisionView while gr-30's fixtures were in flight; one fixture line fixed in the merge commit. The agent found the data cannot show a re-asked decision's old title (gr-35) and resolved a real ambiguity (push-back rows look like review rows) with a tested discriminator. |
| gr-34 + gr-35 Small backend fixes | sonnet | Yes: 370/370, typecheck 0, e2e passing before committing | No | My boundary list missed one test file with a strict equality on the shape being extended; the agent fixed the fixture and said so. |
| gr-31 Grill with docs | opus | Yes: 402/402, typecheck 0, e2e passing before committing; the CLI's directory confinement reproduced by the main session with an escape attempt denied | No | I named a skill file absent from the repo and pointed at the wrong document for the --bare trade-off; the context is built at five call sites, not the one I named. The agent found that only --restricted confines file tools and that --bare (which would skip CLAUDE.md discovery) is unusable with OAuth, and documented both. |
| gr-15 Tree layout prototypes | sonnet | Yes: 417/417, typecheck 0, e2e passing before committing; screenshots reviewed | No | A fresh database needs migrations before a seed script can insert (boot once, stop, seed, restart); now in the UI boilerplate. The agent added wheel zoom beyond the brief because the graph was unjudgeable without it, and said so. |
| gr-33 Round-size nudge | sonnet | Yes: 426/426, typecheck 0, e2e passing before committing | No | None. |
| gr-32 Bulk reopen from a document | opus | Yes: 452/452, typecheck 0, e2e passing before committing; the real-model proof reproduced and handled the case that broke the orchestrator's own script | No | The progress panel was told to reuse polling that lives in a file I put off-limits; the agent made it self-polling. Behaviour after a failed batch was unspecified; the agent chose the safe option and said so. |
| gr-36 Remove the tree prototypes | sonnet | Yes: 437/437, typecheck 0, e2e passing before committing; no references left | No | I named the wrong commit as the pre-prototype baseline; the agent found the right one and said so. |
| gr-1tj Reject duplicate-title proposals | sonnet | Yes: 442/442, typecheck 0, e2e passing on the merged tree; the diff read and the two modified pre-existing tests checked for weakened assertions | No | Named the one test likely to break (an assertion on the collision message) but not the two that actually broke first: a test helper defaults every proposed decision's title to the same string, so any test proposing two decisions in one round tripped the new check before reaching the frontier and cycle rejections it asserts on. The agent found them by running the suite and fixed them by giving distinct titles, assertions intact. |
| gr-7wg.1 Model-lock helper (change-model feature) | sonnet | Yes: 448/448 and typecheck 0 re-run in the worktree; diff exactly the two files named | No | Nothing. The prompt fixed the file path and export name so later tickets could depend on them. |
| gr-7wg.2 Turn launch reads the row's model | sonnet | Yes: test-only, merged main 449/449, typecheck 0; the agent showed its test failing against a hard-coded model first | No | Nothing. The main session had already found that every call site reads the row and the fake records `context.model`, so the ticket became a confirmation. The skill split out a ticket with almost no work in it, which it could not have known. |
| gr-7wg.4 Derived `modelLocked` flag | sonnet | No on attempt 1: it stopped at its precondition. Attempt 2 yes: merged main 460/460, typecheck 0 | No | That Agent-tool worktrees branch from `origin/main`, not local `main`, so a ticket that depends on merged-but-unpushed work starts without it. Prompts now open with `git merge --ff-only main`. The precondition guard stopped a second `isModelLocked` from being written. |
| gr-7wg.3 `set-session-model` action | sonnet | Yes: merged main 460/460, typecheck 0; the agent showed the lock cases failing with the check disabled | No | Nothing blocking. The prompt said nothing in the codebase uses transactions and told the agent to stop rather than improvise if they were unsupported; it established support from the framework source and templates. |
| gr-7wg.5 Header model select | sonnet | No: tests and typecheck passed, but the main session's `agent-browser` check (fake-interviewer instance, sync routes blocked to hold a tab stale) measured the model-locked message on screen for 28 ms before the refresh replaced it (story 12) | No: the same agent fixed it on a follow-up message; re-verified all five browser checks; merged main 460/460, typecheck 0, e2e passing | That the refusal message must survive the refresh; the agent resolved story 6 against story 12. Unit tests could not see it, and live sync hides a stale tab unless its events and poll routes are blocked. By the user's decision the shipped form departs from the spec: a toast, not an inline message. |
| gr-ksx.1 Project registry (export-anywhere feature, PR #1) | opus | Yes: 499/499, typecheck 0, e2e passing; `agent-browser`: a non-git folder refused inline, registering from a subfolder resolved the git root with `just check` and ignored visibility, and the target repo was left with 0 changes | No | That the output page renders only for confirmed sessions, so its project select was first checked through the action and clicked in the browser only in gr-ksx.4. The first ticket under the PR flow: push, PR and `gh` auth all worked from the worktree. |
| gr-ksx.3 `just register-project` (PR #2) | sonnet | Yes: recipe run by the main session from the worktree against a throwaway database: root defaulted from a subfolder, relative `--root` resolved, a plain folder refused with exit 1 | No | Nothing. |
| gr-ksx.2 Tracker front-matter (PR #3) | sonnet | Yes: 516/516, typecheck 0, e2e passing; `agent-browser`: valid block pre-filled folder and slug, invalid block showed a diagnostic naming `tickets_dir`, and refresh re-read only on request | No | The agent stopped before pushing because another session had switched the `gh` account; the main session pushed. Follow-up carried into gr-ksx.4: the containment check was lexical, so a symlink could escape. |
| gr-ksx.4 Explicit export with preview (PR #4) | opus | No on one point: `agent-browser` showed re-export would delete a hand-written `issues/99-my-notes.md`. Fixed by the same agent with a manifest (only files the export wrote are removed); re-verified 521/521 and in the browser | No | The brief specified the destructive removal rule literally ("delete `issues/*.md` not in the plan"); it should have limited removal to files the app owns. Symlink containment, the preview/write match and the output-page project select all verified in the browser. |
| gr-ksx.6 Waves from `blockedBy` (PR #5) | sonnet | Yes: 539/539, typecheck 0, e2e passing; `agent-browser`: wave badges, a cycle refused inline with nothing saved, and clearing a blocker gave one wave | No | Nothing. The epic correction (reuse `blocked_by_json`, no new column) held: one shared cycle check. UX nit: the cycle message ends with a developer phrase. |
| gr-ksx.5 Visibility report (PR #6) | sonnet | Yes: 546/546 (564 combined with gr-ksx.6), typecheck 0, e2e passing; `agent-browser`: untracked reported with the exact add/commit/push remedy, ignored with `check-ignore -v` plus a flag-mismatch warning, and 0 files staged | No | Nothing. The one PR in this feature the agent opened itself, because the `gh` account happened to be correct. |
| gr-ksx.7 HANDOFF.md and briefs (PR #7) | opus | Yes: 594/594, typecheck 0, e2e passing; `agent-browser`: generate, empty brief slots, edit (Edited, not Stale), overwrite confirm, a blocker edit turned it Stale, and export wrote HANDOFF.md and briefs with `{{BUNDLE}}` filled in and Export stale after an edit | No | That staleness has to cover blocker edits: `set-ticket-blocked-by` does not touch `tickets_generated_at`, which the brief said up front. A dialog overlay that looked stuck turned out to come from the main session's long-lived browser session, not the app (gr-03p, not reproducible in a clean session). |
| gr-ksx.8 Export gate and generate-all (PR #8) | sonnet | Yes: 598/598, typecheck 0, e2e passing; fresh `agent-browser` session: Export disabled with a reason, and `export-session` itself refused `handoff-missing` / `handoff-stale` with nothing written; Generate everything made the handoff current without writing | No | Nothing. Behaviour change to note: a session with no tickets can no longer export, since it can never have a handoff. |
| Export-anywhere feature, process findings | n/a | n/a | n/a | (1) Agent-tool worktrees start from `origin/main`; merging each ticket through a PR keeps that base current (`.claude/rules/worktrees.md`). (2) Another session on this machine kept switching the `gh` account, so agents stopped before pushing on 6 of 8 tickets and the main session pushed. (3) The live dev server applies new migrations as soon as merged code is pulled into the main checkout, before any restart: harmless while migrations are additive. (4) Browser verification needs a fresh `agent-browser` session per check and controls targeted by `data-testid`; a reused session with stray clicks produced a false bug. (5) The spec was grilled without codebase access and asked to rebuild existing dependency storage; the three epic corrections caught it before delegation. |
| gr-pt3.1 Readiness request kind, action, storage, idea editing (PR #9) | opus | Yes: 616/616, typecheck 0, re-verified in the worktree and on merged main | No | Nothing blocking. The agent gave the judge its own conversation so the interviewer model stays unlocked, validated only `ready` verdicts against the rule, and named the busy refusal `turn-working` where existing actions say `turn-in-progress`; ticket 02 had to handle both. |
| gr-pt3.2 Readiness panel, editable idea, list badge (PR #10) | opus | Yes: 630/630, typecheck 0; browser check on an isolated port-8090 instance of the worktree with the real interviewer: assess rendered every field, list badge carried the verdict, edit and save cleared the result, starting the interview removed the edit and assess controls | No | Nothing blocking. The repo has no DOM test harness, so the component tests render server-side and the click flow rests on the browser check; a failed readiness turn's Retry starts the interview instead of re-assessing, because the turn kind is not stored. |
| Idea-readiness feature, process findings | n/a | n/a | n/a | (1) Built without a grilling session: the spec came from a failed session plus a four-idea calibration spike, and both tickets passed first time, so a concrete brief with evidence can skip the interview. (2) The browser check needs the worktree's own server on another port with `AUTH_DISABLED` and an isolated `DATABASE_URL`, since the main dev server runs main's code. (3) The judge is lenient on one-liners under Fable (gr-htk): it quoted the goal sentence as evidence. |
| gr-7jg.1 Turn, run and attempt records (PR #13) | sonnet | Yes: 644/644 in the worktree; 669/669 on a trial merge with PR #12 and again on merged main, typecheck 0 | No | Three gaps. The turns and sessions tables reference each other, which Drizzle cannot type, so the session-side foreign keys live only in the migration SQL. Stale review and supersession have no row of their own, so the session links to their latest turn. The table-count test sat outside the file boundaries. |
| gr-7jg.2 Distinct per-call outcomes from the interviewer port (PR #12) | opus | Yes: 655/655 in the worktree, then 669/669 on merged main, typecheck 0; diff confined to `server/interviewer/` | No | Scoping the `gh` identity per command with `GH_TOKEN` was blocked by the gh-identity hook and the worktree guard; `gh auth switch` before each command worked, and the account still flipped between push and PR. The ticket left open how to classify non-JSON output and a missing conversation id; the agent reported both as schema-invalid. `pnpm typecheck` prints config warnings but exits 0. |
| gr-7jg.3 Record every attempt of a propose-round turn (PR #14) | opus | No: review found interviewer errors stored with kind null, the same as a running attempt; the same agent added an `error` attempt kind on the same PR. Then 678/678 in the worktree, 691/691 on a trial merge with PR #15 and on merged main, typecheck 0 | No | The attempt kinds had no value for interviewer errors, and the shared constants were outside the file boundaries. Attempt numbers count resume fallbacks, so "attempt N of 3" must skip them (ticket 06). A refused call reads as success until relabelled a moment later. Queued one-at-a-time rounds carry no turn. The worktree permission hook blocks heredocs and inline scripts. |
| gr-htk A restated goal is not readiness evidence (PR #15) | sonnet | Yes: 682/682 in the worktree, 691/691 on a trial merge with PR #14 and on merged main, typecheck 0 | No | The calibration inputs the prompt named were problem statements, which by construction state no solution: sonnet returned no objective for one of them and fable invented one. The bug case (the books one-liner) is not-ready with zero evidence on both models. The readiness action's tests live in `request-next-round.test.ts`, not a file of their own. |
| gr-7jg.4 Record attempts for the other five turn kinds (PR #17) | sonnet | Yes: 703/703 in the worktree, 710/710 on a trial merge with PR #16 and on merged main, typecheck 0; every `askUntilAccepted` caller records a turn | No | Nothing missing. The stale-review tests live in `submit-round.test.ts`. Its supersession check nested inside a propose-round turn got a turn record of its own, which exposed a seam with gr-7jg.5 that neither ticket could see alone. |
| gr-7jg.5 A manual retry continues the same turn (PR #16) | sonnet | No: review found retry detection would revive any stopped turn of the same kind while the session was failed, hours later; then PR #17's nested turn showed that "latest turn" by start time picks the nested one. Both fixed on the same PR: a retry joins only the session's most recently completed turn. Then 698/698, 710/710 on the trial merge and merged main, typecheck 0 | No | The ticket never defined which stopped turn a retry joins, and a pre-existing test asserted the old behaviour. The seam with gr-7jg.4 was found by the main session reading both PRs, not by either agent: the same shape as every between-ticket defect in the PoC. |
| gr-7jg.6 Attempt log for round proposals (PR #18) | sonnet | No: its own fourth screenshot showed a stopped turn with an attempt still labelled RUNNING and a ticking timer, which a server restart mid-call would produce for real; the same agent rendered it as Interrupted on the same PR. Then 723/723 on a trial merge and merged main, typecheck 0 | No | How the live status finds the running turn (it added a `get-latest-turn` read action) and the failed-turn panel (added, so a failed proposal's log is reachable). The fake interviewer has no latency, so a live turn can only be shown with seeded data. It drove the user's Chrome instead of `agent-browser`, on its own port and database. Screenshots cannot be attached to a PR through `gh`. |
| gr-7jg.7 Attempt log for the other five turn kinds (PR #20) | sonnet | Yes on its own: 733/733, typecheck 0, browser check on its own port; e2e passing on main plus PR #20 alone | No | The canned fake does not script readiness or stale review, so those two were seeded for the browser check; the fake's queue is process-wide, and one mismatched click corrupts it until restart. It added a read-only helper to `turn-records.ts`, which the prompt had put off-limits, without saying so. |
| gr-7jg.8 Smoke test covers the attempt log (PR #19) | sonnet | No: on the trial merge with PR #20 the browser test failed, because its locator assumed one attempt log per page and #20 added a second in the done panel. The same agent merged main, added a `round-history` test id and scoped the locators. Then e2e, 733/733 and typecheck 0 on main plus #19 and on merged main | No | Nothing missing in the brief; the failure was a between-ticket seam that only the combined tree showed. The live turn state is unobservable with a zero-latency fake, so the test asserts the collapsed log. |
| Turn-visibility feature, process findings | n/a | n/a | n/a | (1) Every review failure except one sat at a seam between two tickets (#16 with #17, #19 with #20), and was caught only by reading both diffs or running the trial merge; each ticket's own suite was green. (2) Machine load (average about 30, a Rancher Desktop VM and other sessions) makes the full suite time out in database setup across unrelated files; `vitest --maxWorkers=3` gives a reliable run. The main session once closed a bead in the same command as the test run, before reading a red result, and had to reopen it. (3) Two agents strayed from their briefs without saying so: one drove the user's Chrome instead of `agent-browser`, one added a helper to an off-limits file. (4) The first outside proposal (issue #11) was built as specced after three corrections against the code; the corrections held. |
| gr-yts.1 Collect the project's server facts (PR #21) | sonnet | No: review found the git guard allowed every form of `log`, and `git log --output=<file>` writes a file; the same agent refused `--output` on the same PR. Then 750/750 on a trial merge and merged main, typecheck 0 | No | The facts shape the main session had given the parallel scout ticket differed from the one this ticket built; the main session sent the real shape across before the scout ticket finished, instead of leaving ticket 03 two types to reconcile. `rev-parse` cannot name the branch of a repo with no commits, so both HEAD and branch are null there. |
| gr-yts.2 Scout request kind in the interviewer port (PR #22) | opus | No: its own verification and a real CLI secrets probe passed, but two of its findings needed fixing before merge. Reads under `.git/` were allowed, and a remote URL there can hold a token; and a denied file looked like a missing one, so the scout reported a committed folder as absent. The same agent denied `.git/` and added a prompt line, re-probed with a credentialed remote, then 795/795 on a trial merge and merged main, typecheck 0 | No | The facts shape changed mid-task (the main session corrected it by message). The request needed a `context` because existing tests read it. The deny flag was checked against the installed CLI before use, and the probe showed one `Read` rule also governs Grep and Glob. Credentials in remote URLs also reach the model through the server facts: moved into ticket 03. |
| gr-yts.3 Scout report storage, action, citation check and staleness (PR #23) | opus | Yes: 825/825 in the worktree, on a trial merge and on merged main, typecheck 0 | No | The busy-turn error code (two spellings exist; it used `turn-working`) and table versus column were left open. The re-run semantics were underspecified: a kept decision is not carried into the next report, and a decision kept two re-runs ago could be lost. Settled in ticket 06 before it runs, not after. Stripping userinfo also drops the user of `ssh://` URLs, which is harmless. |
| gr-3uc.1 Session-scoped scenarios in the fake interviewer (PR #24) | opus | Yes: 839/839 on main plus #24 and on merged main, typecheck 0, e2e passing | No | Whether the session id was required was unstated; it made it required, so typecheck proves every request builder sets it, which is what surfaced the conflict with #25. `just dev-fake` passes no database or auth settings, so a worktree agent has to run its command by hand. The `AGENTS.md` action list and a stale comment in `playwright.config.ts` were outside its files. |
| gr-yts.4 Readiness with a project: scout first, sourced evidence (PR #25) | sonnet | Yes on its own; the trial merge with #24 conflicted in `scout-project.ts`, where #24 added the session id to code #25 had moved into a shared function. The same agent merged main and set it there. Then 852/852, typecheck 0 and e2e on main plus #25 and on merged main | No | It flagged, this time, the one line it changed outside its files. Open points it settled and were accepted: repo citations are checked whatever the verdict, and the list badge stays idea-only to avoid a git call per row. The conflict was the predicted one; a required field made it loud rather than silent. |
| gr-3uc.4 Record real interviewer answers and replay them (PR #26) | sonnet | Yes: 864/864 on a trial merge onto current main and on merged main, typecheck 0 | No | Whether a bad recording throws or returns a typed failure was open; it throws, like the fake's scenario lookup. The recording switch is read through the adapter's env option, identical to `process.env` outside tests. |
| gr-yts.5 Repo decisions in the design tree, project context for every turn (PR #27) | opus | Yes: 868/868 in the worktree, 880/880 on a trial merge onto current main and on merged main, typecheck 0, e2e passing | No | The widest ticket in the scout, and the one expected to leave a seam; it did not, because the project context is a required request field and typecheck forced all seven builders to carry it. Gaps it settled: nowhere to record the replaced statement on reopen (it stores the repo statement once, never rewritten), "current report" undefined (keep and drop act on the latest), and a re-scout resetting dispositions (already assigned to ticket 06). Adding an answer kind forced a label and a string in the app, which it flagged. |
| gr-3uc.2 Scenarios for every request kind (PR #28) | sonnet | Yes: 871/871 in the worktree, 887/887 on a trial merge onto current main and on merged main, typecheck 0, e2e passing | No | It chose to prove each scenario through the real actions rather than the fake alone, which is what shows a scenario passes the app's own tree, citation and readiness rules. The scout scenario cites three files the scout's browser test must plant in its fixture repository; carried into that ticket. |
| gr-yts.6 Re-scout and drift (PR #29) | sonnet | No: review found that a dropped or undecided proposal the scout reported `unchanged` but did not propose again would vanish from the new report, trusting the prompt to repeat it; the same agent carried it forward in the app. Then 891/891 on a trial merge and merged main, typecheck 0 | No | The agent found that reopening a decision inside the scout's own turn fails with `turn-in-progress`, because the turn holds the session; drift is applied after the turn ends. The brief's "dispositions carry forward" read as if kept decisions lived in the report; they live in the tree. |
| gr-3uc.3 A browser test for each flow (PR #30) | sonnet | Yes: e2e 6/6 three times in its worktree; 891 unit tests, typecheck 0 and e2e 6/6 twice on a trial merge, and again on merged main | No | The first end-to-end browser runs of these flows found two bugs in code already merged and unit-tested: the live attempt log, the core of turn visibility, is never seen while a turn runs, because the page starts polling only after a fetch already says the turn is working; and the what-changed digest hides behind the round that triggered it. Both verified in the route source and filed as gr-93t and gr-pcd. Both are seams between server and page timing that no action-level test could see: the harness paid for itself on its first run. |
| gr-pcd What-changed digest shows after the review it describes (PR #31) | sonnet | Yes: 896/896, typecheck 0; e2e 6/6 on a trial merge, once on a separate port because another worktree held the fixed port; merged main 896/896 | No | It compared the review's own recorded time rather than the reopen time, and rejected the decision's settle time because it equals the triggering round's stamp. It found a flaky smoke-test locator (filed gr-a66). The fixed e2e port made parallel worktrees' browser runs collide and report nothing, which the main session first had to recognise as a collision rather than a pass (filed gr-4bh). |
| gr-93t Show the live attempt log while a turn runs (PR #32) | sonnet | Yes on unit tests and typecheck; its browser verification was blocked twice by test infrastructure, first the shared port and then a flaky smoke locator, both fixed in parallel (PR #33). 896/896, typecheck 0 and e2e 6/6 three times on a trial merge with #33, and on merged main | No | Its handback was cut off twice mid-verification, and each time it reported the unfinished state honestly rather than claiming green. Fixing the client exposed a server seam that had been invisible (gr-auw): a manual retry's continued turn keeps its completion stamp. |
| gr-4bh, gr-a66 Parallel-safe browser tests (PR #33) | sonnet | Yes: e2e 6/6 five times in a row and two concurrent runs both passing; 896/896, typecheck 0, e2e 6/6 three times on a trial merge with #32 and on merged main | No | It could not reproduce the locator's multi-match alone, but gr-93t's run had hit it (six matches); scoping to the page's own header was right either way. `DEVELOPING.md` still describes a one-test suite, flagged as doc drift outside its files. |
| gr-auw A manual retry's turn reads as running (PR #34) | sonnet | Yes: 897/897, typecheck 0, e2e 6/6 five times on a trial merge and again on merged main | No | The fix was one write in `addRun`. It worked out, and flagged rather than silently chose, that the live panel during a retry now shows the whole turn, not just the running attempt. One of its intermediate browser runs failed on the first live-log assertion under heavy load: the 2 s scenario delay against a 500 ms poll is a thin margin on a busy machine (filed as a follow-up). |
| gr-yts.7 Scout report in the readiness panel, repo marker in the tree (PR #35) | sonnet | Yes: 906/906 in the worktree, 907/907, typecheck 0 and e2e 6/6 twice on a trial merge and on merged main | No | It first built a self-contained panel, found it untestable under the repo's static-render-only component tests, and refactored it to be prop-driven with the route owning the data. The ticket asked that tests show "keep and drop call their actions", which static rendering cannot, so the click wiring moves to ticket 08's browser test. The design tree's tooltip renders nothing statically, so the citation is also a data attribute. |
| gr-dgu Widen the live-log test's timing margin (PR #36) | sonnet | Yes: 897/897; five e2e runs all passing with two more suites running at the same time; 907/907, typecheck 0 and e2e 6/6 twice on a trial merge | No | Raising the delay pushed the one unit test that waits in real time past vitest's 5 s default; it needed its own timeout, which only a full run shows. With three suites at once, session creation itself can time out in `e2e/support.ts`: a machine ceiling to remember before blaming a scenario for a flake. |
| gr-yts.8 Browser smoke test and a real scout run (PR #37) | opus | Yes: 907/907, typecheck 0 and e2e 7/7 three times in the worktree; the same, with e2e 7/7 twice, on a trial merge and on merged main | No | The browser test moved keep and drop's click wiring out of gr-yts.7 as planned. Checking the real run's report against the code: 7 of 8 decisions hold, and all 7 recorded ones quote their lines verbatim. The one inferred decision ("scoutProjectCore is invoked only through the scout-project action") is false, because `assess-readiness` calls it too. The citation check proves the lines exist, not that an "only" claim is true (gr-o5p). |
| gr-o5p Scout may not infer exclusivity from one citation (PR #38) | sonnet | Yes: 908/908 and typecheck 0 in the worktree, on a trial merge and on merged main | No | A one-rule prompt change with a phrase test. The test proves the prompt carries the rule, not that the model obeys it; only the next real scout run shows that. |
| gr-3uc.5 A recorded demo of the whole app (PR #39) | opus | No: its branch conflicted with #37 in the fake's scenario map and hard-coded `/opt/homebrew/bin/ffmpeg`. After one fix-back: 908/908, typecheck 0 and e2e 7/7 twice on a trial merge with the demo excluded, and 908/908 on merged main | No | The brief assumed a fresh session with an empty tree. With repo decisions kept, the workspace offers "Ask for the next round", not "Start the interview". A non-exact `Accept` locator also matched real choice labels containing "accepted". `pnpm test:e2e -- --project=…` did not reach Playwright, so the `justfile` calls `playwright test --project` directly. The agent found the user's dev server with `pgrep -f "agent-native dev"`, took it for its own and killed it. The brief said nothing about process ownership; the rule is now in `.claude/rules/worktrees.md`. The long real recording also tripped the handback guard twice, and the agent needed resuming each time. |
| gr-qs7.2 Tracked decisions.md files in the project facts (PR #40) | sonnet | Yes: 913/913 and typecheck 0 in the worktree, on a trial merge and on merged main | No | The brief missed that the stored scout report re-parses facts through a schema that strips unknown keys, so a new facts field broke an unrelated round-trip test. The agent added one optional line to that schema, outside its boundary, and flagged it rather than weakening its own tests. Its comments name ticket ids, which go stale; ticket 03 rewords them. |
| gr-qs7.1 decisions.md in the export plan (PR #41) | opus | Yes on tests: 921/921 and typecheck 0. One fix-back for a spec gap, not a defect: 928/928, typecheck 0 and e2e 7/7 on a trial merge, 928/928 on merged main | No | It found what the grilling missed. Accepting a recommendation stores the recommendation's reasoning, not the choice's label, so "why only on a text match" would have left the most common answer kind with no label and no why. It built the spec literally and asked instead of guessing; the user chose the recommended choice's label and rationale. A dropped repo decision never enters the tree, so the planner seam cannot test its exclusion; it holds by construction. |
| gr-qs7.4 The smoke test covers decisions.md (PR #42) | sonnet | Yes: e2e 7/7 in the worktree, twice on a trial merge, typecheck 0 | No | The canned interview already settles two qualifying decisions, so no scenario change was needed; the smoke test now registers a throwaway git project so export can run at all. |
| gr-qs7.3 The scout reads decisions.md as recorded, and honours Supersedes (PR #43) | sonnet | Yes: 932/932, typecheck 0 and e2e 7/7 on a trial merge with #42, and 932/932 on merged main | No | `assess-readiness` needed no change: it reaches the facts through the same scout core, which the brief guessed wrong. It made the facts field required and let the stored schema default it, so reports saved before it still parse. Like the exclusivity rule, the prompt test proves the rule is sent, not obeyed; gr-qs7.5 checks that. |
| gr-qs7.5 A real decisions.md round trip (PR #44, spike) | opus | Partly: 4 of 5 checks pass. The main session checked both saved scout reports: session 2 proposes six exported decisions as recorded, each cited to decisions.md; session 1's re-scout sees no decisions.md | No | Check 3 fails: session 2 also proposed the ADR-003 statement its decisions.md entry supersedes, cited to the very line the entry names as its Source. The prompt carried the Supersedes rule and the model did not follow it. A prompt rule is not a guarantee; the fix moves it into the citation check (gr-qs7.10). The run also found that scouting refuses a confirmed session, so re-scouting after export needs a decision added first. |
| gr-qs7.6 intent.md in the export plan (PR #45) | sonnet | Yes: 937/937 and typecheck 0 in the worktree; the same and e2e 7/7 on a trial merge; 937/937 on merged main | No | Planning intent.md on every export changed exact file lists in two tests outside its boundary; it fixed the assertions and flagged them. Evidence lines read "the repo's statement" where the grilling said "the repo shows"; the wording is fixed in gr-qs7.7, which touches the same renderer. |
| gr-qs7.10 Refuse scout proposals that cite a superseded source (PR #46) | sonnet | Yes: 950/950 and typecheck 0 in the worktree, on a trial merge and on merged main | No | The brief said to read decisions.md "at HEAD, the same way citations are checked"; citations are in fact checked against the working tree, and the agent followed the code and flagged the contradiction rather than inventing a git-show path. The prompt rule that the real run ignored is now enforced by the server's citation check. |
| gr-qs7.7 Provenance manifest and the edited-file guard (PR #47) | opus | Yes: 954/954 and typecheck 0 in the worktree; 967/967, typecheck 0 and e2e 7/7 twice on a trial merge; 967/967 on merged main | No | The brief kept it off the UI, but renaming the result's `files` to `written` forced a one-token fix in the export section, flagged. One existing test edited spec.md by hand and expected it overwritten: under the guard that is now an override, which is the feature working. It left `grill-room/AGENTS.md` describing the old manifest because the file was outside its boundary; that doc drift goes to the next ticket. |
| gr-c3y just demo also regenerates the README preview (PR #48) | sonnet | Yes: the WebP step alone reproduced the committed preview byte for byte, and `just --list` parses | No | Two cosmetic leftovers, not worth a round trip: `mktemp` leaves its empty base file in TMPDIR (only the .gif is trapped), and `just --list` shows the recipe's last comment line, which now reads mid-sentence. |
| gr-qs7.8 Edited files in the export preview (PR #49) | sonnet | Yes: 971/971, typecheck 0 and e2e 7/7 in the worktree and on a trial merge; 971/971 on merged main | No | The permission classifier refused its `gh pr create` as an out-of-place publication; it stopped and reported instead of working around it, and the user added an allow rule. Its browser check used the claude-in-chrome tools, which drive the user's own Chrome: the rule said "agent-browser", but not that the Chrome tools were off limits, so the rule now names them. The app had no Checkbox primitive, so it added shadcn's with the house icon set. |
| gr-qs7.9 The smoke test covers intent.md and the guard (PR #50) | sonnet | Yes: e2e 7/7 twice in the worktree and twice on a trial merge, typecheck 0, 971/971 on merged main | No | The preview already exposed `data-edited` and the override checkbox's test id, so the test needed no app change; stable selectors chosen in gr-qs7.8 paid off here. |
| gr-5e7.4 The handoff-scout request kind (PR #51) | opus | Yes: 1008/1008 and typecheck 0 in the worktree, re-run by the reviewer and on a trial merge | No | First ticket through the adversarial review gate: the opus reviewer approved and marked it ready, with seven non-blocking findings the builder's report did not mention, among them a pre-existing path bug (a one-letter first folder is refused, filed), a 40-ticket cap that makes large handoffs fail every retry, and a required file that forces the scout to invent one for no-code tickets. The last two are folded into gr-5e7.5. |
| gr-5e7.1 Delivery recipe and review switch on the project (PR #52) | sonnet | No: 981/981 and typecheck 0, but the reviewer requested changes. After one fix round: 983/983 on the branch; 1020/1020, typecheck 0 and e2e 7/7 on a trial merge and on merged main | No | The review gate's first catch. Adding the new fields to the handoff fingerprint changed its hash for every project, so every existing handoff would have gone stale on upgrade and blocked export; 981 passing tests never exercised "an old handoff after upgrade". The fix includes each field only when it differs from its default, and the reviewer recomputed the old hash on main to prove the pinned literal was not derived from the branch. The brief named the risk to the reviewer, not to the builder; a brief that adds a fingerprint field should say "existing rows must keep their hash". |
