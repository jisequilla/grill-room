# Grill Room brand: Ember on the Ledger

The contract is `grill-room/DESIGN.md`. This folder holds its assets:

- `grill-room-mark.svg`: the Grate mark.
- `brand-sheet.png`: the wordmark, palettes and state stamps, in light and dark.
- `mockups/`: Stitch mockups in the Stitch project "Grill Room — Decision Workbench (phase 1)" (`projects/14147914545929672477`):
  - `stitch-01`: session in a round (`371f3eae2d7a45879523aa1166e19c2b`). It still shows the old single-bar logo and no progress segments; the SVG mark is the reference.
  - `stitch-02`: done panel (`65209274f9ba4700ba371beff07e53d4`).
  - `stitch-03`: project page (`3ea693a7fa224675802670ab209bc280`).
  - `stitch-04`: sessions list with the Sessions and Projects sidebar (`bb7b43d3543a46f8be29ea82efa37fe5`).
  - `stitch-05`: a round on mobile, 390px (`1048562075684c83a2d9e39528619459`).

  Stitch sets buttons and tree rows in mono; the contract keeps mono for labels.
- `before/`: the UI as it was before the redesign, captured with the fake interviewer.
